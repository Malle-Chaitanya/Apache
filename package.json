{
  "name": "itsm-migrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI-assisted ITSM migration platform — moves the whole application (data + configuration) from Zendesk to Freshdesk.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/api/server.js",
    "dev": "node --watch src/api/server.js",
    "check": "node --check src/api/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^8.6.0"
  }
}
